// app/layout.tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Bizora – Slimme boekhouding voor teams",
  description:
    "Bizora is de moderne web app voor facturen, offertes, BTW en meer – gebouwd met Supabase & Vercel.",
  icons: [{ rel: "icon", url: "/favicon.ico" }],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="nl">
      <body style={{ margin: 0, fontFamily: "Inter, system-ui, Arial, sans-serif", color: "#0f172a" }}>
        <header style={{ padding: "16px 24px", borderBottom: "1px solid #e5e7eb", display: "flex", alignItems: "center", justifyContent: "space-between" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            <svg width="28" height="28" viewBox="0 0 24 24" aria-hidden>
              <path fill="currentColor" d="M12 2L2 7l10 5l10-5zm0 7L2 4v13l10 5l10-5V4z" />
            </svg>
            <strong style={{ letterSpacing: 0.2 }}>Bizora</strong>
          </div>
          <nav style={{ display: "flex", gap: 16 }}>
            <a href="#features" style={{ textDecoration: "none", color: "#334155" }}>Features</a>
            <a href="#pricing" style={{ textDecoration: "none", color: "#334155" }}>Pricing</a>
            <a href="#contact" style={{ textDecoration: "none", color: "#334155" }}>Contact</a>
          </nav>
        </header>
        <main>{children}</main>
        <footer style={{ padding: "24px", borderTop: "1px solid #e5e7eb", textAlign: "center", fontSize: 14, color: "#64748b" }}>
          © {new Date().getFullYear()} Bizora. All rights reserved.
        </footer>
      </body>
    </html>
  );
}
